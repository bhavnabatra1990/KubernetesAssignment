apiVersion: v1
kind: Service
metadata:
  name: employeeapp-service
  namespace: applicationnamespace
  annotations:
    dev.okteto.com/auto-ingress: "true"
    service.beta.kubernetes.io/load-balancer-source-ranges: "34.171.29.207/80"
spec:
  type: ExternalName
  externalName: employeeservice.com
  selector:
    app: EmployeeApp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer