apiVersion: v1
kind: Service
metadata:
  name: employeeapp-service
  namespace: applicationnamespace
  annotations:
    dev.okteto.com/auto-ingress: "true"
    service.beta.kubernetes.io/load-balancer-source-ranges: "34.171.0.0/24,10.240.0.0/16"
spec:
  type: ExternalName
  externalName: employeeservice.com
  selector:
    app: EmployeeApp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
  type: LoadBalancer