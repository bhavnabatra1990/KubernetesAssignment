apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-ingress
spec:
  rules:
    - host: employeeservice.com
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: EmployeeApp
                port:
                  number: 80
